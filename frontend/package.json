{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "runFrontAndBack": "concurrently \"npm run dev\" \"cd ../backend && ./mvnw spring-boot:run\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vite": "^7.1.2"
  },
  "dependencies": {
    "@pdftron/webviewer": "^11.7.1"
  }
}